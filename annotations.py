# ANNOTATIONS 
# @desc: USED FOR ADDING TYPES TO DATA OPTIONALLY (TypeScript for Python)
# types are ignored by python, but are useful for keeping track of types
# Lkke JS, Python is dynamically typed at runtime, so specifications are unnecessary

def increment(n: int) -> int:
    return n + 1

count: int = 0
